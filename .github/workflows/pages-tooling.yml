name: Pages tooling

on:
  pull_request:
    paths: ['.github/**']
  push:
    branches: [main]
    paths: ['.github/**']

permissions:
  contents: read

concurrency:
  group: pages-tooling-${{ github.ref }}
  queue: max

jobs:
  test:
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Test artifact isolation and preview lifecycle
        run: python3 -m unittest discover -s .github/tests -v
